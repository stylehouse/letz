settings {
   nodaemon   = true,
}
sync{
    default.rsyncssh,
    source="/home/s/stylehouse/G/Y/",
    host="sa",
    targetdir="/home/s/src/letz/src/lib/Y",
    delay=0.3,
    rsync = {
        -- resolve links (dont link)
        _extra = {"-L"},
    },
    -- must be this way round
    filter = {"+ *.ts","- *"},
}
